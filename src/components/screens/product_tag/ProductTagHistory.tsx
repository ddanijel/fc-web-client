import React from 'react';
import ProducerBottomTagNavigation from "../producer/ProducerBottomTagNavigation";

const ProductTagHistory = ({match}) => {
    return (
        <div>
            Product tag history

            <ProducerBottomTagNavigation/>
        </div>
    );
};

export default ProductTagHistory;